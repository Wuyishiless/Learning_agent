# AI Agent 面试知识问答

## Q1：是否了解 AI Coding 领域的专业概念，如 Harness（测试基准/沙箱）、Loop Engineering（循环迭代工程）？

　Harness 是指包裹 LLM 的执行脚手架层，负责把模型的“意图”转化为“可执行、可验证的动作”。具体包括工具调用的接口定义、代码执行沙箱、执行结果的结构化回传、权限/安全边界控制。可以理解成“Agent 的操作系统”——LLM 负责推理决策，Harness 负责落地执行和环境交互。

　Loop Engineering 是指对 Agent 的核心迭代循环做系统性工程优化，是 ReAct 范式的工程化升级而非替代。核心解决的问题是：单纯的 Reason-Act-Observe 循环在长任务中会出现上下文膨胀、错误累积、缺乏自我纠错等问题。Loop Engineering 通过引入上下文管理策略、阶段性验证（Verify）、失败重试、循环终止条件设计等机制，让循环本身具备“自我调节”的能力，从而支撑更长、更复杂的任务执行。

　Tool Use 是模型侧的能力（决定调用哪个工具、传什么参数），Harness 是工程侧的基础设施（工具怎么注册、怎么执行、结果怎么解析回传）。

---

## Q2：请介绍一下常见的 Agent 工作流范式（如 ReAct、Plan-and-Execute 等）的区别与适用场景。

　**ReAct（Reason + Act）**

　  思考与行动交替进行，每步都基于最新观察重新推理下一步动作。优点是适应性强，适合环境不确定、需要持续根据反馈调整策略的任务，如工具检索、网页交互。缺点是长任务中上下文膨胀、成本高，容易偏离目标或累积错误。

　**Plan-and-Execute**

　  先做一次性完整任务分解，生成子任务序列，再依次执行，执行阶段可以使用轻量模型，仅在偏差较大时重新规划。优点是效率高，适合结构清晰的长链路任务，如代码库级别的多文件重构。缺点是对动态环境的适应性较弱，原有计划可能过时。

　**Reflexion / 自我反思循环**

　  在 ReAct 基础上加入“自我评估—反思—修正”环节。执行失败后不是简单重试，而是先生成对失败原因的反思文本，作为下一轮尝试的上下文补充。适合需要多次试错，且失败信息本身有价值的任务，如代码调试、单元测试驱动开发。

　**Multi-Agent / Supervisor-Worker 编排**

　  把任务拆给不同角色的子 Agent，如 Planner、Coder、Reviewer，由一个 Supervisor 负责协调。适合任务边界清晰、可并行、需要不同专业能力的复杂工程任务，但通信开销和协调复杂度更高。

　实际选择时，可以重点看两个维度：**任务结构是否可预知**，决定更偏向 Plan-and-Execute 还是 ReAct；**是否需要试错和反思机制**，决定是否引入 Reflexion。

---

## Q3：从产品和技术愿景角度看，你认为一个理想的 AI 智能助手应具备哪些核心能力？它与传统的客服机器人或 FAQ 知识库检索系统有何本质区别？

　AI 智能助手区别于 FAQ 系统的核心，不只是“有没有思考能力”，而是思考之后能否对现实世界做动态、可执行、可验证的操作。这需要两层能力配合。

　**Harness 层：**赋予模型“操作现实”的手，即根据当前意图动态选择并调用外部接口。这里的“动态”是关键，接口不是提前配置死的规则树，而是运行时根据语义理解现场决定调什么、传什么参数。

　**后置校验层：**因为动态调用天然带来不确定性，如参数错误、接口异常、返回内容与预期不符，所以还需要在每次调用后加入结果校验和纠错机制。可以是规则校验（Schema / 业务约束），也可以是类似 Reflexion 的思路，让模型对失败结果生成反思，指导下一轮重试或改用其他工具。

　**ReAct：**把“推理—调用—观察”这个循环显式结构化，让“动态选接口”和“看结果决定下一步”这两件事在同一个可追踪的循环里完成，是承载上述两层能力的具体工作流骨架。

　三者合起来才构成一个完整闭环：**Harness 提供“能不能做”，后置校验保证“做得对不对”，ReAct 提供“怎么循环着做”。**

　这正是它和 FAQ 系统的本质区别——FAQ 没有动态执行这一步，自然也不需要复杂的执行后校验，因为答案通常已经提前确定。

---

## Q4：在 AI 应用架构中，旨在统一大模型与外部数据源或工具集成接口的连接协议（如 Model Context Protocol / MCP），其本质和核心设计思想是什么？

　MCP（Model Context Protocol）的本质，是一种标准化的大模型与外部工具、数据源之间的连接协议，核心思想是通过统一的协议和接口，把 Agent 与具体工具实现解耦。

　如果不使用 MCP，Agent 往往需要针对不同工具单独实现 Tool Call 的接口和适配逻辑。当工具数量增加，或者同一个工具需要被不同 Agent 复用时，就容易产生大量重复的集成代码和接口适配。

　MCP 通过定义统一的通信规范，使不同的 Agent 或 AI 应用可以按照相同的方式发现和调用外部能力，而具体工具的实现则由 MCP Server 负责。这样就实现了调用方与工具实现的解耦，提高了工具的复用性和系统的可扩展性。

　从架构上看，MCP 主要包括 **Host、MCP Client 和 MCP Server**。Host 是承载 AI 应用的主体，比如一个 Agent 应用；Client 负责与 MCP Server 建立连接并进行协议通信；Server 则负责封装具体的工具、数据源或其他能力，并按照 MCP 规范对外暴露。

　所以可以简单理解为：**MCP 就像 AI 应用和外部能力之间的一层标准化“适配协议”**，让 Agent 不需要关心每个工具具体是怎么实现的，只需要按照统一协议进行发现和调用。

---

## Q5：Agent 的工具调用是局限于单一服务，还是需要编排和操作多个跨域的异构服务？

　Agent 的工具调用几乎不可能局限于单一服务，因为现实世界的任务本身就是跨域的。比如“查一下这只股票的行情，结合最新新闻做判断，再把结果推送到我的微信”，这一个任务就横跨了行情数据、新闻检索、消息推送三个完全异构的系统。

　所以更准确的说法是：**Agent 天然需要编排和操作多个跨域异构服务**，这不是“要不要”的问题，而是“在哪个粒度上怎么做”的问题。

　**第一个层次是单个 Agent 内部如何处理跨域调用：**通过 MCP 这类统一协议，把不同域的服务包装成同构的 Tools 接口，Agent 在 ReAct 循环里根据当前意图动态做工具选择。本质上是把“要不要跨域”这个问题下沉成“每一步该调用哪个已注册工具”这样更细粒度的决策，调用逻辑本身是统一的，不需要提前区分单服务还是多服务场景。

　**第二个层次是多 Agent 协同：**只有当任务复杂到需要不同专业能力分工协作，而不仅仅是调用不同工具时，才会引入 A2A 这类协议，用于 Agent 之间的能力发现和任务委派。这一层通常还会配合 Plan-and-Execute 做顶层任务拆分，把子任务分发给对应领域的子 Agent 去执行。

　这两个层次不能混为一谈。很多团队会误以为“跨域”就等于要上多 Agent 架构，但实际上大部分跨域编排场景，单个设计良好的 Agent 加统一工具协议层就完全能覆盖，没必要为了“看起来更高级”而引入多 Agent 的通信和状态同步开销。

　工程上真正容易踩坑的地方，反而不是协议层面，而是随着单 Agent 挂载的工具数量增多，模型在候选工具里做选择的准确率会明显下降，容易选错工具或者填错参数。这时候通常需要引入**工具检索或者分层路由**，先按业务域筛出一小批候选工具，再让模型在小范围里做选择。

---

## Q6：在大模型应用落地中，确定性工作流（Workflow）与自主智能体（Agent）在业务场景选择、决策机制及容错成本上有何本质区别？如何理解自主智能体（Agent）与工作流（Workflow）的区别、联系及适用场景？

　Workflow 和 Agent 最大的区别，在于**决策权掌握在谁手里**。

　Workflow 是开发者预先定义好执行路径和业务逻辑，系统按照确定的流程执行。例如每天早上自动查询天气，再生成天气播报，这种任务目标明确、流程固定，就比较适合 Workflow。它的优点是可控、稳定、容易测试和排查问题。

　Agent 则把一部分决策权交给大模型，由模型根据任务目标、当前上下文和工具执行结果，自主决定下一步应该做什么、调用什么工具以及什么时候结束。因此它更适合流程不固定、需要动态决策和多步骤协作的任务。比如复杂的股票信息分析，需要综合行情、新闻、财务数据，并根据中间结果动态决定下一步操作。

　两者并不是完全对立的。实际的 AI 应用中通常是 **Workflow + Agent 结合**：用 Workflow 负责确定性的流程和边界，用 Agent 负责其中需要动态决策的环节。

　从容错成本来看，Workflow 因为路径确定，所以行为更容易预测和测试，出现错误时也比较容易定位；Agent 的决策具有一定的不确定性，可能出现错误调用、循环、工具选择不合理等问题，因此通常需要增加重试、超时、人工确认、权限控制、状态检查等机制，所以它的工程控制成本和错误风险通常更高。

　所以可以简单理解为：**Workflow 强调“按照预先设计好的流程执行”，Agent 强调“根据目标自主决定怎么完成任务”。**

---

## Q7：单 Agent 系统与多 Agent 系统（Multi-Agent）各自的优缺点是什么？在实际业务中，如何评估和选择使用单 Agent 还是多 Agent 架构？

　**单 Agent 优点：**架构简单、延迟低、成本可控、调试和可观测性好，一条执行轨迹出错时容易定位。

　**单 Agent 缺点：**随着工具数量和上下文长度增加，工具选择准确率和推理质量可能下降；不同领域的信息混在同一个上下文里，容易产生“上下文污染”，互相干扰推理；如果任务包含多个独立子任务，也可能因为串行执行导致延迟较高。

　**多 Agent 优点：**可以把不同领域的职责拆给专门的子 Agent，每个子 Agent 上下文更干净、工具集更聚焦；独立子任务可以并行执行；单个子 Agent 出错的影响范围可以被隔离，不会直接污染整个系统的上下文。

　**多 Agent 缺点：**通信协调成本会随着 Agent 数量增加而上升；每个子 Agent 都可能产生额外的推理和调用成本；调试更困难，错误可能出在某个子 Agent 内部，也可能出在 Agent 间的通信或结果整合环节；还存在协调失败的风险，比如子 Agent 之间对任务边界理解不一致，导致结果冲突或重复劳动。

　实际项目里可以从几个维度判断是否需要多 Agent：

　**1. 任务是否可以清晰拆解：**如果子任务边界明确、耦合度低，可以考虑多 Agent；如果子任务之间需要频繁共享中间状态、强依赖顺序，拆成多 Agent 可能只会增加通信开销。

　**2. 子任务是否可以并行：**如果拆开后能够并行执行，多 Agent 可能带来延迟方面的收益；如果本质上仍然是串行任务，拆分后的主要成本可能来自额外通信和推理。

　**3. 单 Agent 是否已经出现质量下降：**例如工具调用错误率上升、上下文过长导致遗漏指令等。如果这些问题已经明显出现，可以考虑通过工具路由、上下文隔离或多 Agent 进行治理。

　**4. 团队是否具备足够的工程能力：**多 Agent 对 tracing、监控、状态管理和故障排查的要求更高，需要结合实际成本和可观测性能力进行选择。

---

## Q8：请挑选一种主流的 Agent 范式（如 ReAct），详细阐述其核心的 Execution Loop（思考-行动-观察循环）与推理机制。

　ReAct 的核心是把推理与行动交替组织起来，用 **Thought → Action → Observation** 的循环替代直接输出答案：模型先根据当前状态进行推理，据此决定调用哪个工具（Action），再拿到真实执行结果（Observation）作为下一轮推理的输入，如此循环直到模型判断任务完成并输出 Finish。

　这个结构的关键价值是 **Grounding**——每一步决策都可以被外部真实反馈校正，而不是一次性生成整条可能脱离现实的执行路径。同时，由于每一步的决策和工具结果都可以记录，也有利于调试和可观测。

　**Reflexion** 是在 ReAct 之上叠加的自我改进层，由 Actor、Evaluator、Self-Reflection 以及 Episodic Memory 四部分组成。

　当 Evaluator 判定任务失败，Self-Reflection 会结合失败原因生成一段具体的反思文本并存入 Memory。下一次 Actor 重新执行 ReAct 循环时，这段反思会作为额外上下文注入，帮助模型规避同样的错误。

　因此，Reflexion 可以理解为一种**语言层面的反馈学习机制**：它不直接更新模型参数，而是通过文本反思让后续尝试利用之前的失败经验。但它的有效性依赖于 Evaluator 能否提供可靠的成败信号；如果任务缺乏明确的验证标准，这套机制的效果就会受到限制。

---

## Q9：一个标准的 AI Agent 架构应该包含哪些核心模块？

　一个标准的 AI Agent 架构可以按 Lilian Weng 提出的经典框架来组织，核心包括 **Planning、Memory、Tool Use、Action**，LLM 是驱动整个系统决策的推理核心，Harness 则是承载这些模块运行的工程基础设施。

　具体执行链路是：用户输入指令后，Planning 模块先进行任务拆解，必要时结合自我反思修正计划；Memory 模块分为短期记忆和长期记忆，短期记忆主要是当前对话和执行轨迹，长期记忆通常通过向量库检索，为当前决策提供历史经验或外部知识。

　LLM 基于 Planning 给出的子目标和 Memory 提供的上下文，决定下一步是否调用工具、调用哪个工具（Tool Use）；Harness 则负责把“调用哪个工具”这个模型决策实际转化为对外部环境的操作，包括工具调用协议、执行沙箱、结果解析和回传，是 LLM 决策和真实世界之间的“翻译层和执行层”。

　工具执行结果作为 Observation 返回给 LLM，进入下一轮决策，直到任务完成。

　可以简单理解为：

　**Planning：想怎么做**

　**Memory：记住什么**

　**Tool Use：决定用什么能力**

　**Harness：负责真正执行**

　**LLM：负责整体推理和决策**

---

## Q10：Agent 生成并执行的处置指令在底层是如何抽象的？包含哪些维度的 Action 原语？

　Agent 生成的处置指令，底层通常不会直接以自然语言执行，而是会抽象成结构化的 **Action**。Action 可以理解为 Agent 决策和实际执行之间的中间表示，通常包含 Action 类型、目标对象、参数、执行条件以及上下文等信息。

　从原语上可以抽象成几类：

　**Read：**读取外部信息。

　**Write：**修改外部状态。

　**Execute：**执行具体操作或调用 Tool。

　**Communicate：**和用户、系统或其他 Agent 通信。

　**Delegate：**把子任务交给其他 Agent。

　**Control：**包括 Wait、Retry、Branch、Stop 等控制类 Action。

　LLM 负责根据任务和上下文生成 Action，Harness 负责对 Action 进行解析、参数校验、权限控制、风险检查和调度，Executor 再调用具体 Tool、API 或其他 Agent 执行，执行结果作为 Observation 返回给 LLM，形成下一轮决策。

　因此可以把整个过程理解为：

　**LLM 负责决策 → Action 负责结构化表达 → Harness 负责控制和编排 → Tool / Agent 负责实际执行。**

　这样可以把模型推理和真实世界的执行隔离开，提高系统的可控性、安全性和可审计性。

---

## Q11：Agent 调用的外部工具链是否具备事务回滚或操作撤销的能力？

　大多数外部工具链本身并不天然具备统一的事务回滚能力，这是 Agent 系统里一个容易被低估的现实约束。

　传统数据库事务通常具有 ACID 特性，一组操作要么全部成功，要么全部回滚；但 Agent 调用的外部工具往往是跨系统、跨服务边界的。例如一个任务里先调用支付接口，再调用库存扣减接口，最后调用消息通知接口，这三个系统各自拥有自己的状态管理机制，不一定存在一个统一的事务协调器把它们组织成一个 ACID 事务。

　因此，跨服务 Agent 操作通常需要通过**补偿机制 + 幂等机制**处理执行失败。

　**Saga 模式（补偿事务）：**不追求真正意义上的“回滚”，而是为每一个正向操作定义对应的补偿操作。例如“扣库存”对应“补库存”，“创建订单”对应“取消订单”。当链路中某一步失败时，系统按照已经执行步骤的逆序调用补偿操作，尽量抵消已经产生的副作用，而不是把整个系统状态真正“倒带”。

　**两阶段确认（Try-Confirm-Cancel）：**把每个操作拆成“预留资源”和“确认生效”两个阶段，先 Try，全部 Try 成功后再统一 Confirm；如果某一步 Try 失败，则执行 Cancel。

　**幂等设计：**即使补偿机制本身失败需要重试，也要避免重复操作导致状态进一步恶化。
    >常见方式包括 `request_id`、`idempotency_key`、状态检查等。

　**Harness：**负责记录执行状态、编排正向操作和补偿操作，控制重试、超时和人工介入。如果 Harness 没有记录“这次任务链路里每一步实际执行了什么、产生了什么可补偿的凭证”，事后想进行补偿也无从下手。因此每个 Action 执行成功后的结果中，应该包含足够用于后续补偿的上下文信息，例如订单 ID、事务 ID 等。

　**风险控制：**对于支付、删除、发送消息等不可逆或高风险操作，需要增加权限校验、限额控制、人工确认等机制。

　所以可以总结为：**Agent 的跨服务执行通常不是依赖传统意义上的“回滚”，而是通过 Saga 补偿、幂等、状态记录和风险控制，实现“失败可恢复”。**
