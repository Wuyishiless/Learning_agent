# Agent 面试知识问答

## Q1：是否了解 AI Coding 领域的专业概念，如 Harness（测试基准/沙箱）、Loop Engineering（循环迭代工程）？

　**Harness** 是指包裹 LLM 的执行脚手架层，负责把模型的"意图"转化为"可执行、可验证的动作"。具体包括工具调用的接口定义、代码执行沙箱、执行结果的结构化回传、权限/安全边界控制。可以理解成"agent 的操作系统"——LLM 负责推理决策，harness 负责落地执行和环境交互。

　**Loop Engineering** 是指对 agent 的核心迭代循环做系统性工程优化，是 ReAct 范式的工程化升级而非替代。核心解决的问题是：单纯的 Reason-Act-Observe 循环在长任务中会出现上下文膨胀、错误累积、缺乏自我纠错等问题。Loop Engineering 通过引入上下文管理策略、阶段性验证（verify）、失败重试、循环终止条件设计等机制，让循环本身具备"自我调节"的能力，从而支撑更长、更复杂的任务执行。

　tool use 是模型侧的能力（决定调用哪个工具、传什么参数），harness 是工程侧的基础设施（工具怎么注册、怎么执行、结果怎么解析回传）。

## Q2：请介绍一下常见的 Agent 工作流范式（如 ReAct、Plan-and-Execute 等）的区别与适用场景。

**ReAct（Reason + Act）**

　思考与行动交替进行，每步都基于最新观察重新推理下一步动作。优点是适应性强，适合环境不确定、需要持续根据反馈调整策略的任务（如工具检索、网页交互）。缺点是长任务中上下文膨胀、成本高、容易偏离目标或累积错误。

**Plan-and-Execute**

　先做一次性完整任务分解，生成子任务序列，再依次执行（执行阶段可用轻量模型），仅在偏差较大时重新规划。优点是效率高、适合结构清晰的长链路任务（如代码库级别的多文件重构）。缺点是对动态环境的适应性弱，计划可能过时。

**Reflexion / 自我反思循环**

　在 ReAct 基础上加入"自我评估-反思-修正"环节，执行失败后不是简单重试，而是先生成对失败原因的反思文本，作为下一轮尝试的上下文补充。适合需要多次试错、且失败信息本身有价值的任务（如代码调试、单元测试驱动开发）。

**Multi-Agent / Supervisor-Worker 编排**

　把任务拆给不同角色的子 agent（如 planner、coder、reviewer），由一个 supervisor 协调。适合任务边界清晰、可并行、需要不同"专业能力"的复杂工程任务，但通信开销和协调复杂度更高。

　**任务结构是否可预知（决定 Plan 还是 ReAct）**，以及**是否需要试错反思机制（决定要不要引入 Reflexion 层）。**

## Q3：从产品和技术愿景角度看，你认为一个理想的 AI 智能助手应具备哪些核心能力？它与传统的客服机器人或 FAQ 知识库检索系统有何本质区别？

　AI 智能助手区别于 FAQ 系统的核心，不只是"有没有思考能力"，而是**思考之后能否对现实世界做动态、可执行、可验证的操作**。这需要两层能力配合：

　**Harness 层**：赋予模型"操作现实"的手，即根据当前意图**动态选择并调用外部接口**——这里的"动态"是关键，接口不是提前配置死的规则树，而是运行时根据语义理解现场决定调什么、传什么参数。

　**后置校验层**：因为动态调用天然带来不确定性（参数错误、接口异常、返回内容与预期不符），所以还需要在每次调用后加一层**结果校验和纠错机制**——可以是规则校验（schema/业务约束），也可以是类似 Reflexion 的思路，让模型对失败结果生成反思，指导下一轮重试或改用其他工具。

　**ReAct 范式**则是把"推理-调用-观察"这个循环显式结构化，让"动态选接口"和"看结果决定下一步"这两件事在同一个可追踪的循环里完成，是承载上述两层能力的具体工作流骨架。

　三者合起来才构成一个完整闭环：Harness 提供"能不能做"，后置校验保证"做得对不对"，ReAct 提供"怎么循环着做"。这正是它和 FAQ 系统的本质区别——FAQ 没有"动态"这一步，自然也不需要"校验"这一步，因为答案早就审核好了。

## Q4：在 AI 应用架构中，旨在统一大模型与外部数据源或工具集成接口的连接协议（如 Model Context Protocol / MCP），其本质和核心设计思想是什么？

　MCP（Model Context Protocol）的本质，是一种标准化的大模型与外部工具、数据源之间的连接协议，核心思想是通过统一的协议和接口，把 Agent 与具体工具实现解耦。

　如果不使用 MCP，Agent 往往需要针对不同工具单独实现 Tool Call 的接口和适配逻辑。当工具数量增加，或者同一个工具需要被不同 Agent 复用时，就容易产生大量重复的集成代码和接口适配。

　MCP 通过定义统一的通信规范，使不同的 Agent 或 AI 应用可以按照相同的方式发现和调用外部能力，而具体工具的实现则由 MCP Server 负责。这样就实现了调用方与工具实现的解耦，提高了工具的复用性和系统的可扩展性。

　从架构上看，MCP 主要包括 Host、MCP Client 和 MCP Server。Host 是承载 AI 应用的主体，比如一个 Agent 应用；Client 负责与 MCP Server 建立连接并进行协议通信；Server 则负责封装具体的工具、数据源或其他能力，并按照 MCP 规范对外暴露。

　所以可以简单理解为：MCP 就像 AI 应用和外部能力之间的一层标准化“适配协议”，让 Agent 不需要关心每个工具具体是怎么实现的，只需要按照统一协议进行发现和调用**。**

## Q5：Agent 的工具调用是局限于单一服务，还是需要编排和操作多个跨域的异构服务？

　Agent 的工具调用几乎不可能局限于单一服务，因为现实世界的任务本身就是跨域的——比如"查一下这只股票的行情，结合最新新闻做判断，再把结果推送到我的微信"，这一个任务就横跨了行情数据、新闻检索、消息推送三个完全异构的系统。所以更准确的说法是：Agent 天然需要编排和操作多个跨域异构服务，这不是"要不要"的问题，而是"在哪个粒度上怎么做"的问题。

　这个问题其实要分两个层次来看。

　**第一个层次是单个 Agent 内部如何处理跨域调用：**通过 MCP 这类统一协议，把不同域的服务包装成同构的 Tools 接口，Agent 在 ReAct 循环里根据当前意图动态做工具选择，**本质上是把"要不要跨域"这个问题下沉成了"每一步该调用哪个已注册工具"这样更细粒度的决策**，调用逻辑本身是统一的，不需要提前区分单服务还是多服务场景。

　**第二个层次是多 Agent 协同：**只有当任务复杂到需要不同专业能力分工协作，而不仅仅是调用不同工具时，会引入 A2A 这类协议，用于 agent 之间的能力发现和任务委派，这一层通常还会配合 Plan-and-Execute 做顶层任务拆分，把子任务分发给对应领域的子 agent 去执行。这两个层次不能混为一谈——很多团队会误以为"跨域"就等于要上多 agent 架构，但实际上大部分跨域编排场景，单个设计良好的 agent 加统一工具协议层就完全能覆盖，没必要为了"看起来更高级"而引入多 agent 的通信和状态同步开销。

　工程上真正容易踩坑的地方，反而不是协议层面，而是随着单 agent 挂载的工具数量增多，模型在候选工具里做选择的准确率会明显下降，容易选错工具或者填错参数，这时候通常需要引入工具检索或者分层路由，先按业务域筛出一小批候选工具，再让模型在小范围里做选择，来缓解这个问题。

## Q6：在大模型应用落地中，确定性工作流（Workflow）与自主智能体（Agent）在业务场景选择、决策机制及容错成本上有何本质区别？如何理解自主智能体（Agent）与工作流（Workflow）的区别、联系及适用场景？

　Workflow 和 Agent 最大的区别，在于决策权掌握在谁手里。

　Workflow 是开发者预先定义好执行路径和业务逻辑，系统按照确定的流程执行。例如每天早上自动查询天气，再生成天气播报，这种任务目标明确、流程固定，就比较适合 Workflow。它的优点是可控、稳定、容易测试和排查问题。

　Agent 则把一部分决策权交给大模型，由模型根据任务目标、当前上下文和工具执行结果，自主决定下一步应该做什么、调用什么工具以及什么时候结束。因此它更适合流程不固定、需要动态决策和多步骤协作的任务。比如复杂的股票信息分析，需要综合行情、新闻、财务数据，并根据中间结果动态决定下一步操作。

　两者并不是完全对立的。实际的 AI 应用中通常是 Workflow 和 Agent 结合：用 Workflow 负责确定性的流程和边界，用 Agent 负责其中需要动态决策的环节。

　从容错成本来看，Workflow 因为路径确定，所以行为更容易预测和测试，出现错误时也比较容易定位；Agent 的决策具有一定的不确定性，可能出现错误调用、循环、工具选择不合理等问题，因此通常需要增加重试、超时、人工确认、权限控制、状态检查等机制，所以它的工程控制成本和错误风险通常更高。

　所以可以简单理解为：Workflow 强调“按照预先设计好的流程执行”，Agent 强调“根据目标自主决定怎么完成任务”。

## Q7：单 Agent 系统与多 Agent 系统（Multi-Agent）各自的优缺点是什么？在实际业务中，如何评估和选择使用单 Agent 还是多 Agent 架构？

　单 agent 优点：架构简单、延迟低、成本可控、调试和可观测性好（一条执行轨迹，出错容易定位）。缺点：**随工具数量和上下文长度增加，工具选择准确率和推理质量会明显下降**；不同领域的信息混在同一个上下文里，容易产生"上下文污染"，互相干扰推理；无法并行执行，如果任务本身包含多个独立子任务，只能串行走完，延迟高。

　多 agent 优点：可以把不同领域的职责拆给专门的子 agent，每个子 agent 上下文更干净、工具集更聚焦，推理质量更稳定；独立子任务可以并行执行，降低总延迟；单个子 agent 出错的影响范围可以被隔离，不会污染整个系统的上下文。缺点：**通信协调成本随 agent 数量增加而快速上升**（你提到的这点是对的）；每个子 agent 都要走一遍推理，总的 token 成本和调用次数成倍增加；调试更难——错误可能出在某个子 agent 内部，也可能出在 agent 间的通信/结果整合环节，定位链路更长；还存在协调失败的风险，比如子 agent 之间对任务边界理解不一致，导致结果冲突或重复劳动。

　实际项目里可以用几个具体维度去判断要不要上多 agent：**任务是否可以清晰拆解成边界明确、耦合度低的子任务**——如果子任务之间需要频繁共享中间状态、强依赖顺序，拆成多 agent 只会增加通信开销，不如单 agent 内部管理好状态；

　**子任务是否可以并行**——如果拆开后能并行跑，多 agent 在延迟上有实打实的收益；如果本质还是串行，拆分带来的主要是成本，收益有限；

　**单 agent 的工具集和上下文是否已经出现质量下降**（比如工具调用错误率上升、上下文过长导致遗漏指令）；

　**对可观测性和成本的容忍度**——多 agent 的调试和成本门槛更高，如果团队没有配套的 tracing/监控能力，贸然上多 agent 反而会让问题更难排查。

## Q8：请挑选一种主流的 Agent 范式（如 ReAct），详细阐述其核心的 Execution Loop（思考-行动-观察循环）与推理机制。

　ReAct 的核心是把推理过程显式化，用 `Thought → Action → Observation` 三段式循环替代直接输出答案：模型先给出对当前状态的推理（Thought），据此决定调用哪个工具（Action），再拿到真实执行结果（Observation）作为下一轮推理的输入，如此循环直到模型判断任务完成并输出 Finish。这个结构的关键价值是"grounding"——每一步推理都被外部真实反馈锚定，而不是让模型一次性生成一整条可能脱离现实的思维链，这也顺带带来了可解释性和调试上的好处，因为每一步的决策依据都留了痕迹。

　Reflexion 是在 ReAct 之上叠加的自我改进层，由 Actor（执行 ReAct 循环的主体）、Evaluator（判断本轮任务成败）、Self-Reflection 模块（结合失败原因生成自然语言反思）、以及 episodic memory（存储历史反思供后续尝试参考）四部分组成。当 Evaluator 判定任务失败，Self-Reflection 会生成一段具体的反思文本存入 memory，下一次 Actor 重新执行 ReAct 循环时，这段反思会作为额外上下文注入，帮助模型规避同样的错误。本质上这是一种"语言层面的强化学习"——用文本反思代替梯度更新，来实现从失败中学习的效果，但它的有效性依赖于 Evaluator 能给出可靠的成败信号，一旦任务缺乏明确的验证标准，这套机制就很难真正发挥作用。

## Q9：一个标准的 AI Agent 架构应该包含哪些核心模块？

　一个标准的 AI Agent 架构可以按 Lilian Weng 提出的经典四模块框架来组织：**Planning、Memory、Tool Use、Action**，LLM 是驱动整个系统决策的推理核心，Harness 则是承载这四个模块运行的工程基础设施。

　具体的执行链路是：用户输入指令后，**Planning 模块**先把任务做拆解（子目标分解），必要时结合自我反思（比如 Reflexion）修正计划；**Memory 模块**分两层——短期记忆是当前对话/执行轨迹的上下文窗口，长期记忆通常通过向量库做检索增强，为当前决策提供历史经验或外部知识；LLM 基于 Planning 给出的子目标和 Memory 提供的上下文，决定下一步要不要调用工具、调用哪个工具（**Tool Use**）；**Harness** 这一层负责把这个"调用哪个工具"的决策，实际转化成对外部环境的操作——包括工具调用协议、执行沙箱、结果解析和回传，是 LLM 决策和真实世界之间的"翻译层和执行层"；工具执行的结果通过**Action/Observation** 反馈回 LLM，进入下一轮循环，直到 Planning 判断任务完成。

## Q10：Agent 生成并执行的处置指令在底层是如何抽象的？包含哪些维度的 Action 原语？

　Agent 生成的处置指令，底层通常不会直接以自然语言执行，而是会抽象成结构化的 Action。Action 可以理解为 Agent 决策和实际执行之间的中间表示，通常包含 Action 类型、目标对象、参数、执行条件以及上下文等信息。

　从原语上可以抽象成几类：Read 用于读取外部信息，Write 用于修改外部状态，Execute 用于执行具体操作或调用 Tool，Communicate 用于和用户、系统或其他 Agent 通信，Delegate 用于把子任务交给其他 Agent，另外还有 Wait、Retry、Branch、Stop 等控制类 Action。

　LLM 负责根据任务和上下文生成 Action，Harness 负责对 Action 进行解析、参数校验、权限控制、风险检查和调度，Executor 再调用具体 Tool、API 或其他 Agent 执行，执行结果作为 Observation 返回给 LLM，形成下一轮决策。

　因此可以把整个过程理解为：LLM 负责决策，Action 负责结构化表达决策，Harness 负责控制和编排，Tool 或 Agent 负责实际执行。这样可以把模型的推理和真实世界的执行隔离开，提高系统的可控性、安全性和可审计性。

## Q11：Agent 调用的外部工具链是否具备事务回滚或操作撤销的能力？

　**大多数外部工具链本身并不天然具备事务回滚能力**，这是 Agent 系统里一个容易被低估的现实约束。传统数据库事务有 ACID 保证，一组操作要么全成功要么全回滚；但 Agent 调用的外部工具往往是**跨系统、跨服务边界**的——比如一个任务里先调用了支付接口、又调用了库存扣减接口、又调用了发短信通知接口，这三个系统各自有各自的状态管理，根本不存在一个统一的事务协调器能把它们串成一个 ACID 事务，因此需要通过**补偿机制 + 幂等机制**处理执行失败。

　**Saga 模式（补偿事务）**：这是最常见的思路。不追求真正的回滚，而是为每一个"正向操作"预先定义一个对应的"补偿操作"——比如"扣库存"对应"补库存"，"创建订单"对应"取消订单"。当链路中某一步失败，系统按已执行步骤的**逆序**依次调用补偿操作，把已经产生的副作用尽量抵消掉，而不是真的把状态"倒带"回去。

　**两阶段确认（Try-Confirm-Cancel）**：把每个操作拆成"预留资源"和"确认生效"两步，先 Try（预占但不真正生效），全部 Try 成功后统一 Confirm，任何一步 Try 失败就统一 Cancel，这样失败发生在早期阶段时代价更小。

　**幂等设计兜底**：即使补偿机制本身失败或重试，只要每个操作都设计成幂等的（重复调用效果等同于调用一次），至少不会因为重试导致状态进一步恶化。

>通过 `request_id` / `idempotency_key`、状态检查等方式。

　**Harness**：负责记录执行状态、编排正向操作和补偿操作，控制重试、超时和人工介入。如果 Harness 没有记录"这次任务链路里，每一步实际执行了什么、产生了什么可回滚的凭证（比如订单 ID、事务 ID）"，事后想补偿也无从下手，所以每个 Action 执行成功后返回的结果里，应该包含足够用于后续补偿的上下文信息。

>风险控制：对支付、删除、发送消息等不可逆操作进行权限校验、限额或人工确认。

# Q13：请详细介绍一个生产级Agent系统的端到端架构设计，以及从输入到输出回写链路中的核心组件

　生产级Agent系统的输入到输出这条链路上每一环要有对应的工程保障——可观测、可回滚、可降级、可审计。一个完整的端到端链路大致是：

`输入接入 → 意图理解与预处理 → 编排层(Planning+Memory) → 推理循环(LLM+Harness+Tool) → 结果校验与回写 → 可观测性闭环`

六个环节缺一不可。

---

## 1. 输入接入层

　生产系统通常是多入口的——CLI、Web、API、IM 机器人（如企业微信/Slack Bot）都可能是同一个 Agent 的前端。

　这一层的职责是把不同渠道的原始输入**归一化成统一的内部请求格式**（用户ID、会话ID、原始文本/结构化参数），并做基础的鉴权和限流。

---

## 2. 预处理/意图理解层

　拿到归一化请求后，先做轻量判断——这是一个可以直接命中缓存/FAQ 的简单问题，还是需要进入 Agent 推理的复杂任务？

　很多生产系统会在这里做**分流**：简单问题走轻量 Workflow 直接返回，只有真正需要动态决策的请求才进入下面的重量级 Agent 循环，这样能大幅降低平均延迟和成本。

---

## 3. 编排层（Planning + Memory）

* **Planning**

  * 对复杂任务先做子目标拆解；
  * 必要时提供给用户一个可见的执行计划。

* **Memory**

  * **短期记忆**：是当前会话的上下文窗口（滑动窗口或摘要压缩，避免无限增长）。
  * **长期记忆**：通常通过向量库做检索增强，把用户历史偏好、历史执行经验作为参考信息注入当前上下文。

---

## 4. 推理执行循环（核心）

　LLM 基于 Planning 给出的子目标和 Memory 提供的上下文，进入 `Thought → Action → Observation` 的 ReAct 循环。

　每一轮的 Action 会被抽象成结构化的 **Action 原语**（目标对象、操作类型、参数、风险等级），交给 **Harness** 处理。

　Harness 负责把这个原语路由到正确的 Tool（可能通过 **MCP** 统一接入的外部服务），在沙箱环境里执行，捕获真实结果。

　如果任务涉及多步跨系统调用，这一层还要维护执行轨迹和可回滚的凭证（订单ID、事务ID），为后续可能的补偿操作做准备。

　执行结果经 Harness 解析成结构化 Observation，反馈回 LLM，进入下一轮，直到 Planning 判断任务完成或触发 Reflexion 式的自我修正。

---

## 5. 结果校验与回写层

　LLM 生成的最终结果在真正返回用户前，通常要经过一层**独立于生成本身的校验**：

* **规则校验**：格式、敏感词、业务约束；
* **语义一致性检查**：结果是否真的回答了原始问题；
* **二次校验**：必要时结合 reranker 或专门的校验模型做二次把关。

　这一点你在 DocVQA 项目里已经实践过，reranker 让准确率从约50%提升到约70%，本质就是这一层的价值。

　校验通过后，回写模块把结果适配回对应的输出渠道格式（比如推送到企业微信要转成卡片消息，API 调用要转成 JSON），完成"回写"这最后一环。

---

## 6. 可观测性闭环

　贯穿全链路的日志和追踪——每一次 LLM 调用、每一次 Tool 调用、每一步 Action 的输入输出都要落地成可查询的 trace。

　用于：

* 事后排查错误；
* 评估工具选择准确率；
* 统计延迟分布。

　这一层不是可选项，是生产系统和 demo 系统的分水岭之一。

# Q14：请设计一个支持高并发、强一致性要求的系统（如高并发点赞/计数系统），简述你的架构设计方案。*

*高并发点赞系统我会把点赞状态和点赞数量拆开设计。点赞状态属于强一致和幂等场景，可以通过用户 ID 加对象 ID 的唯一索引保证一个用户只能点赞一次；点赞数量属于高频写场景，可以接受短暂的最终一致，因此使用 Redis 做实时计数，MQ 异步削峰，再由消费者批量落库。*

*如果出现热点 Key，可以进一步把一个对象的点赞计数拆成多个分片，写入时打散，读取时汇总。读取侧走本地缓存加 Redis，减少数据库压力。最后通过 MQ 消费幂等和定时对账，解决重复消费、消息丢失以及 Redis 与数据库之间的计数漂移问题。*

*所以核心就是：状态保证幂等和正确性，计数保证高并发和高可用，两者采用不同的一致性策略。*

# Q15：当前 AI Coding 方案在迈向全自动软件工程（Fully Automated Software Engineering）时，面临的核心瓶颈与优化方向是什么？

真实软件工程是长程、非确定、缺乏明确终止信号的任务，而现在的 agent 系统在"长程可靠性"和"正确性验证"这两件事上还没有解决好。  

**验证信号的缺失（这是最根本的瓶颈）**：写代码容易，但"这段代码是不是对的"这件事，在复杂真实场景里往往没有一个廉价、可靠的自动化信号。单测能覆盖的只是已知场景，真正的软件工程正确性还包括代码风格、架构一致性、对未预见边界情况的处理——这些没有天然的 reward signal，导致 Loop Engineering 里的"后置校验"环节缺少可靠的判断依据，Agent 很容易在"自己觉得做对了"但实际引入了隐患的状态下终止循环。  

**长程任务的上下文管理与错误累积**：一个真实的软件工程任务动辄几十上百轮工具调用，上下文会不断膨胀，之前提到过的"工具数量增多、上下文变长导致选择准确率下降"这个问题在长程任务里会被无限放大——早期一个小的理解偏差，会在后续几十轮迭代里被不断放大，而不是被及时纠正。  

**非确定性带来的可复现性问题**：这个和 Workflow vs Agent 的区别是一脉相承的——Agent 的执行路径是运行时动态生成的，同一个任务两次运行可能走完全不同的路径，不好复现和定位错误根因。  

**多 agent 协同的边际收益递减**：真实工程任务天然适合拆给不同角色（写代码、写测试、review），但之前聊过的通信协调成本问题在长链路任务里会显著放大——agent 之间对任务边界理解不一致、上下文没有及时同步，反而会引入新的错误来源。  

优化方向：

**把验证做成首要任务，而不是事后补丁**：比如让 Agent 在生成代码的同时同步生成测试,用执行结果（而不是模型自己的判断）作为唯一可信的校验信号,这样"后置校验"就有了确定性的依据,而不是靠另一个 LLM 去判断"这个结果对不对"。

**Loop Engineering 要往"可中断、可恢复、可审计"方向优化**：比如引入 checkpoint 机制，长程任务中间状态可以被保存、回滚到某一步重新分叉尝试,而不是整个循环失败了只能从头再来。

**风险分级驱动的人机协作,而不是全有全无的自动化**：低风险操作（格式化、注释补全）完全自动化,中风险操作（新增功能）自动执行但要求可回滚,高风险操作（数据库变更、生产部署）强制人工确认——这本质是把之前聊的 Action 原语风险等级机制,系统性地应用到整个软件工程流程里。

**专业化子 agent 按需拆分,而不是一开始就上复杂多 agent 架构**：只在真正独立、可并行、职责边界清晰的环节（比如测试生成和主代码逻辑）才拆分,避免为了"看起来更先进"引入不必要的协调成本。

**Benchmark/Harness 本身也要进化**：现在很多评测集覆盖的还是相对简单、单文件、任务边界清晰的场景,离真实的仓库级、长期维护、多人协作的软件工程场景还有差距,这也是为什么"测试基准/沙箱"这个概念本身,现在也是 AI Coding 领域一个专门被拿出来讨论的方向——评测跟不上,优化方向也容易跑偏。